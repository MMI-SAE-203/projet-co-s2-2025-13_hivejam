---
import PocketBase from 'pocketbase';

const pb = new PocketBase('http://127.0.0.1:8090');
const cookies = Astro.cookies;

// Supprime l'auth
pb.authStore.clear();
cookies.delete('pb_auth', { path: '/' });

// Redirection vers la home ou login
return Astro.redirect('/');
---